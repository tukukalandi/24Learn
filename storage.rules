rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow anybody to read
      allow read: if true;
      // Allow authenticated admins to write
      // We check if the user is authenticated and potentially check a custom claim or document
      allow write: if request.auth != null;
    }
  }
}
